steps to create backend
1. generate pacakage.json file
        npm init -y
        write description (optional)
        change in main index to server.js
        convert in type common js to module(used for import and export)
2. create http server
        -Install express module
                npm install express
        -Import express module
                -create server.js(manually)
                import express(import exp from 'express')


npm install -g nodemon //for global installation -g 
nodemon is used to restart server automatically always when we change server